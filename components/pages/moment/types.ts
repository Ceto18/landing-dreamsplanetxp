export interface Photo {
    id: number
    destination: string
    title: string
    image: string
    description: string
    place: string
    experience: string
    moment: string
    emotion: string
    recommendation: string
    gallery: string[]
}